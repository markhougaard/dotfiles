# Shared git config, linked on every platform.
# Editor and diff/merge tools differ per platform; see the [include] at the bottom.
[user]
	name = Mark Hougaard
	email = mark@hougaard.email
	signingkey = D42687F214D07A9F
[github]
	user = markhougaard
[color]
	ui = true
[push]
	default = simple
	followTags = true
	autoSetupRemote = true
[pull]
	rebase = false
[init]
	defaultBranch = main
[core]
	pager = diff-so-fancy | less --tabs=4 -RFX
	excludesfile = ~/.gitignore_global
[interactive]
	diffFilter = diff-so-fancy --patch
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	required = true
	process = git-lfs filter-process
[url "ssh://git@heroku.com/"]
	insteadOf = https://git.heroku.com/
[alias]
	update = !git pull && git submodule update --init --recursive
	new = !git init && git symbolic-ref HEAD refs/heads/main
[commit]
	# gpgsign = true
[gpg]
	program = gpg
[color "diff-highlight"]
	oldNormal = red bold
	oldHighlight = red bold 52
	newNormal = green bold
	newHighlight = green bold 22
[color "diff"]
	meta = 11
	frag = magenta bold
	func = 146 bold
	commit = yellow bold
	old = red bold
	new = green bold
	whitespace = red reverse
# Platform-specific overrides. Later values win, so this must stay last.
[include]
	path = ~/.gitconfig.local
