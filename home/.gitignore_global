# Global gitignore, referenced by core.excludesfile in ~/.gitconfig.
.DS_Store
.AppleDouble
.LSOverride
._*
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Claude Code's per-project local settings, never meant to be committed.
# This rule used to live in ~/.config/git/ignore, which git reads in addition
# to this file - a second global-ignore source no other machine would have had.
**/.claude/settings.local.json
